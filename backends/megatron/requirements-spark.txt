# Setup2 dependency set for the ARM64 Spark nodes.
# Install torch==2.10.0 from the PyTorch cu130 index before this file; a plain
# requirements install does not select the CUDA wheel variant.
torch==2.10.0
megatron-core==0.19.0
# Bridge 0.6.0 declares the 0.46.0rc1 ModelOpt dependency; 0.46.0 stable is
# the verified runtime candidate and is intentionally recorded as a deviation.
nvidia-modelopt==0.46.0
transformers>=5.8,<=5.12.1
datasets==5.0.0
huggingface_hub==1.7.1
pytest>=8,<10

# megatron-bridge==0.6.0 is intentionally NOT listed here. A plain `pip install`
# of it unconditionally requires fast-hadamard-transform, flashinfer-python,
# flashinfer-cubin, comet-ml, mlflow, timm, open-clip-torch and qwen-vl-utils
# (none behind an extra -- `[recipes]` only adds nemo-run, it is not the cause).
# fast-hadamard-transform's PyPI sdist has no csrc/ source at all and none of
# these ship an aarch64 wheel, so any normal install of megatron-bridge fails
# on ARM64 regardless of extras (see docs/backends/megatron.md#spark-environment).
# None of the above are used by this repo's Qwen/GLM SFT path. Install bridge
# itself with --no-deps, then this file for the sub-dependencies it actually
# needs at import time (confirmed against a working install):
#   pip install --no-deps megatron-bridge==0.6.0
#   pip install -r requirements-spark.txt
peft==0.20.0
diffusers==0.36.0
mistral-common==1.11.7
accelerate==1.14.0
einops==0.8.2
omegaconf==2.3.1
tensorboard==2.21.0
typing-extensions==4.16.0
rich==15.0.0
wandb==0.27.2
six==1.17.0
regex==2026.9.3
pyyaml==6.0.3
tqdm==4.70.0
hydra-core==1.3.2
nvidia-resiliency-ext==0.6.0

# Transformer Engine 2.18.0 was built from source for ARM64 with
# NVTE_WITH_NCCL_EP=0 because the installed Torch tree lacks the optional
# symm_mem/nccl_dev_cap.hpp header. It is not asserted as a portable pip
# requirement here; see docs/backends/megatron.md and scripts/spark_runtime_env.sh.
